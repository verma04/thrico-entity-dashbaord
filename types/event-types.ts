export type EventType = "virtual" | "in_person" | "hybrid" | "ONLINE" | "IN_PERSON" | "HYBRID";

export enum EventTypeEnum {
  VIRTUAL = "virtual",
  IN_PERSON = "in_person",
  HYBRID = "hybrid",
  ONLINE_LEGACY = "ONLINE",
  IN_PERSON_LEGACY = "IN_PERSON",
  HYBRID_LEGACY = "HYBRID",
}
